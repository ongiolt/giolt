{
	"name": "api",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev --config wrangler.dev.toml",
		"test": "gleam test",
		"deploy": "wrangler deploy --minify"
	},
	"dependencies": {
		"@clerk/backend": "^1.31.4"
	},
	"devDependencies": {
		"wrangler": "^4.4.0"
	}
}
