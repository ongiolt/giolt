{
	"name": "giolt-api",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev --config wrangler.dev.toml",
		"test": "bun run build.bun.ts && gleam test",
		"migrate:local": "drizzle-kit generate && wrangler d1 migrations apply giolt-db --local",
		"studio": "drizzle-kit studio",
		"visualizer": "drizzle-lab visualizer",
		"deploy": "wrangler deploy --minify"
	},
	"dependencies": {
		"@clerk/backend": "^1.31.4",
		"drizzle-kit": "^0.31.1",
		"drizzle-lab": "^0.11.0",
		"drizzle-orm": "^0.43.1"
	},
	"devDependencies": {
		"wrangler": "^4.4.0"
	}
}
