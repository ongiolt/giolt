{
	"name": "giolt",
	"version": "1.0.0",
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"@types/bun": "latest",
		"typescript": "^5.8.3",
		"wrangler": "^4.14.4"
	},
	"private": true,
	"scripts": {
		"web:dev": "bun run --filter 'web' dev",
		"web:build": "bun run --filter 'web' build",
		"web:preview": "bun run --filter 'web' preview",
		"api:dev": "bun run --filter 'api' dev",
		"docs:build": "bun run --filter 'docs' build",
		"docs:dev": "bun run --filter 'docs' dev",
		"docs:preview": "bun run --filter 'docs' preview",
		"check": "biome check . && bun run --filter 'docs' astro check && bun run --filter 'web' astro check",
		"check:ci": "biome ci . && bun run --filter 'docs' astro check && bun run --filter 'web' astro check",
		"check:fix": "biome check --write . && bun run --filter 'docs' astro check && bun run --filter 'web' astro check"
	},
	"workspaces": ["packages/*"]
}
